export const questionListSelector = (store) => store.question.questionList;

export const hasMoreSelector = (store) => store.question.hasMore;

export const isQuestionLoadingSelector = (store) => store.question.isLoading;

export const pageNumberSelector = (store) => store.question.pageNumber;
