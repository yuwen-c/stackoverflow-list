export const tagListSelector = (store) => store.tag.tagList;

export const isTagLoadingSelector = (store) => store.tag.isLoading;

export const selectedTagSelector = (store) => store.tag.selectedTag;
